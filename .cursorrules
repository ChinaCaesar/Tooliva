# Project Cursor Rules - Desktop Toolbox

## 技术栈基线（必须遵循）

- 核心技术栈固定为：`Tauri 2 + Vue 3 + TypeScript + Rust（Python 补充） + FFmpeg + SQLite`。
- AI Agent 在实现功能时，必须优先使用与该技术栈一致的方案，不引入无关主框架。

## 分层职责约束

- `Vue 3 + TypeScript`：负责页面 UI、交互逻辑、状态管理、国际化与路由。
- `Rust`：负责 Tauri Command、系统 API 调用、文件系统与性能敏感逻辑。
- `Python`：仅作为补充能力，用于脚本化任务、算法原型、可复用处理管线；上线能力优先 Rust 落地。
- `FFmpeg`：统一承担媒体转码/探测/封装处理，不重复造轮子。
- `SQLite`：统一承担本地结构化持久化（任务、历史、配置、索引）。

## AI Agent 开发策略

- 新功能设计时，先判断归属层：
  - UI/体验问题：优先在 Vue 层实现。
  - 本地系统能力或高性能需求：优先 Rust + Tauri Command。
  - 媒体处理：优先 FFmpeg 管线。
  - 数据持久化：优先 SQLite。
  - 快速验证或辅助处理：Python 补充实现，后续按需迁移 Rust。
- 不得默认引入远程服务依赖；优先离线本地可运行方案。
- 方案说明中需明确：数据流、调用链、错误处理、可观测性（日志/状态）。

## 代码与文档规范

- 默认使用 TypeScript 严格类型，不使用 `any` 逃逸（必要时需说明原因）。
- 对复杂函数、公共 API、跨层调用入口补充 `JSDoc` 注释。
- 媒体任务与数据库变更必须补充最小可验证步骤（手动或自动测试说明）。

## 数据与任务模型建议

- 转码任务至少包含：输入文件、输出格式、参数快照、任务状态、进度、错误信息、时间戳。
- SQLite 至少维护：任务表、历史表、应用配置表（可后续扩展）。
- FFmpeg 执行应统一封装，避免页面层直接拼接命令。

## 兼容性与安全

- 路径处理必须兼容 Windows 环境，避免硬编码分隔符。
- 所有文件读写和外部命令执行都要做异常捕获与用户可读错误提示。
- 对用户输入（路径、文件名、参数）进行基础校验后再下发到命令层。
