## 1. 数据模型与配置

- [x] 1.1 在 `home.config.ts` / `types` 中扩展或新增：主推工具卡（含占位卡）、价值卖点横条项、侧栏三卡（安全要点列表、会员权益列表、更新日志条目）的视图模型结构，并与现有核心工具标识对齐以便路由与搜索复用。
- [x] 1.2 在 `useHomePageData.ts` 中输出上述视图模型，避免在 `HomePage.vue` 内硬编码列表；占位卡与会员按钮行为用可配置字段或单一处理函数承载。

## 2. 组件拆分与实现

- [x] 2.1 新增问候与主推区组件（时段问候、副标题、可选装饰图），文案与键名接入 `vue-i18n`。
- [x] 2.2 新增横向主推工具卡列表组件，替换或移除对 `CoreToolGrid` 的依赖；支持「立即使用」操作与占位卡交互，事件向页面层冒泡或 emit。
- [x] 2.3 新增价值卖点横条组件（固定数量图标+标题+短描述），数据来自配置。
- [x] 2.4 以三张独立组件或子目录实现侧栏：安全说明卡、会员权益卡（含占位主按钮行为）、更新日志卡；移除或停用旧 `SideInfoPanel` 中与规格不符的统计/快捷区块，避免重复占位。
- [x] 2.5 按需调整 `HomeTopBar.vue`、`HomeFooterBar.vue` 与参考稿一致的文案层级、搜索占位与快捷键提示；确认与 Tauri 自定义标题栏不重叠。
- [x] 2.6 按需调整 `RecentUsageList.vue` 样式密度（间距、字号）以适配一屏布局，行为保持与数据源一致。

## 3. 页面编排与一屏布局

- [x] 3.1 在 `HomePage.vue` 中重组模板：顶栏 → 中间行（主列 flex:1 + 侧栏固定宽）→ 页脚；中间行设置 `min-height:0` 与 flex/grid，落实 `design.md` 中的布局骨架。
- [x] 3.2 在视口宽度与高度均不低于 1200×720 时验收无页面级纵向滚动；必要时微调内边距、卡片最小高度与最近使用区高度上限。
- [x] 3.3 实现并文档化低于阈值时的断点降级（栈式布局或区域内滚动），在浏览器开发者工具与 Tauri 窗口缩放下手动验证无裁切不可达控件。

## 4. 国际化与资源

- [x] 4.1 为新增与变更区块补充中英文（及项目已有语言）`locale` 键值，避免页面硬编码中文。
- [x] 4.2 若使用装饰图或新图标，在 `homeAssets.ts` 中集中登记路径并优化体积格式（如 WebP/PNG）。

## 5. 可访问性与收尾

- [x] 5.1 为主推卡与占位元素使用语义可聚焦控件并补充 `:focus-visible` 样式；核对搜索快捷键文案与实际快捷键逻辑（Windows / macOS 差异可在文案或逻辑中择一落地并在 `design.md` Open Questions 关闭项中注明）。
- [x] 5.2 运行前端构建与本地 `tauri dev` 冒烟：首页导航、工具卡跳转、顶栏搜索选中、会员占位点击无异常控制台错误。
