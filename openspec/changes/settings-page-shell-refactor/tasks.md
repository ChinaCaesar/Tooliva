## 1. 壳层组件抽取与目录搭建

- [x] 1.1 新增应用壳层目录 `src/layouts/app-shell`，导出 `AppShellLayout`、`AppSidebar`、`AppFooter` 与壳内复用的 `HomeTopBar`（`show-window-controls`）等组合入口，并完成基础样式占位。**验收标准：** 可在 Story 或临时路由中单独渲染壳层且主内容插槽可插入任意测试节点。
- [x] 1.2 将首页现有侧栏、顶栏、底栏 markup 与样式迁移至壳层子组件，首页改为引用壳层且视觉与迁移前一致。**验收标准：** 首页手测导航、搜索选工具、底栏文案与迁移前无回归。
- [x] 1.3 抽取或新建导航配置（路由列表、图标、i18n 键），使侧栏仅依赖配置与当前路由，避免在多个页面重复定义导航项。**验收标准：** 修改配置一处即可增删侧栏项且类型检查通过。

## 2. 路由与设置页接入壳层

- [x] 2.1 调整路由或页面布局，使 `SettingsPage` 与首页共用同一 `AppShellLayout`（或等价嵌套），移除设置页对 `ToolPageTopBar` 的独占依赖。**验收标准：** 进入设置页时侧栏「设置」高亮，顶栏搜索与窗口控件可用。
- [x] 2.2 核对从工具页进入设置后的返回行为，保证历史记录与 `ROUTE_PATHS` 一致且无死循环。**验收标准：** 自工具页经全局入口打开设置再返回，路由栈行为符合产品预期。

## 3. 设置页卡片与行组件

- [x] 3.1 实现 `SettingsSectionCard` 与 `SettingsRow`（或等价命名），支持标题、可选描述、右侧控件插槽。**验收标准：** 至少三个设置项复用该行组件且样式统一。
- [x] 3.2 按设计稿搭建双列栅格：左列常规、输出、缓存；右列更新、隐私、关于；响应式下改为单列堆叠且不重叠。**验收标准：** 默认窗口宽度下布局与设计稿一致，缩窄窗口时无横向溢出滚动条（除非主内容区 intentional）。
- [x] 3.3 实现主题分段控件并与 store 绑定；补充 i18n 中英文键。**验收标准：** 切换主题立即生效且刷新后状态与持久化策略一致。

## 4. Store、路径与危险操作

- [x] 4.1 在 `settings.store` 中扩展新开关与字段（开机自启、托盘、关闭确认、输出目录、缓存目录、更新与隐私相关等），区分「已接 Tauri」与「仅 UI 占位」并在代码注释或常量中标注。**验收标准：** 已有语言、窗口尺寸、任务提醒行为不变；新字段有默认值且不抛异常。
- [x] 4.2 实现路径行组件：展示路径、`更改`（目录选择器或占位）、`打开`（调用 Tauri 或浏览器能力，不可用时提示）。**验收标准：** 在能力可用环境下可打开资源管理器；不可用时用户可见提示。
- [x] 4.3 实现「清除本地数据」危险按钮样式与确认对话框，取消路径无副作用。**验收标准：** 符合 `settings-dashboard` 规格中危险操作场景。

## 5. 关于区块与收尾

- [x] 5.1 实现关于卡片：应用图标、名称、版本、描述、外链或按钮；版本号取自单一配置源。**验收标准：** 版本与关于信息一致；链接可点击或有明确占位行为。
- [x] 5.2 全量走查 i18n、无障碍属性（按钮 `aria-label`、开关语义）、与 `openspec verify` 或项目既有检查命令。**验收标准：** 无新增控制台错误；规格中的场景可手测覆盖。
